using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CheatsSystem : MonoBehaviour
{
    public static bool modeInvincible = true;   // M + I        //modo invencible, el jugador no pierde vida
    public static bool addCompanion = true;     // M + A + C
    public static bool removeCompanion = true;  // M + R + C 
    public static bool selectLevel1 = true;     // M + L + 1
    public static bool selectLevel2 = true;     // M + L + 2
    public static bool selectLevel3 = true;     // M + L + 3
}